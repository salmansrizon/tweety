# Supabase (config DB + pgvector store)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=   # ETL only -- never expose to the client browser

# Google Gemini (ETL only -- text-embedding-004)
GEMINI_API_KEY=

# OpenRouter (web RAG API only -- free model router)
OPENROUTER_API_KEY=

# Google Cloud (ETL only -- BigQuery service account key, JSON)
GCP_SA_KEY=

# Reddit API (ETL only -- PRAW script app credentials)
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=
